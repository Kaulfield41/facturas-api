[project]
name = "facturas-api"
version = "0.1.0"
description = "Extracción estructurada de datos de facturas con LLM y validación Pydantic"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.122.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.3",
]
